package org.idpass.smartscanner.lib.scanner.config

object Language {
    const val NAME = "name"

    // Kinds
    const val EN = "en"
    const val AR = "ar"

    // Locale
    object Locale {
        const val RTL = "RTL"
        const val LTR = "LTR"
    }
}